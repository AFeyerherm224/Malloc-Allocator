# Malloc-Allocator
A lightweight implementation of malloc, free, and free-list management in C using sbrk(). Includes support for block splitting, first-fit allocation, and explicit free list coalescing to reduce fragmentation.
